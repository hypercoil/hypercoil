# Pinned doc-toolchain versions. Single source of truth for bootstrap.sh,
# activate.sh, and CI. Bump deliberately; commit the change.
QUARTO_VERSION=1.9.38
# Pagefind + Python figure deps are pinned in requirements-docs.txt
# (installed into the venv); PAGEFIND_VERSION here is informational.
PAGEFIND_VERSION=1.5.2
