# Python deps for the federation doc build. Installed into the bootstrap venv.
# - pagefind[extended]: federation-wide search index (post-build over _site/);
#   the [extended] extra bundles the platform pagefind binary.
# - jupyter stack + pyyaml + psutil: Quarto's engine for executing {python}
#   cells (figures); Quarto's bundled jupyter shim imports yaml and psutil.
# - numpy/matplotlib: lightweight, CI-safe executable figures (no JAX/HPC).
# Top-level pins; exact transitive versions are frozen to
# requirements-docs.lock.txt by bootstrap.sh for full reproducibility.
pagefind[extended]==1.5.2
jupyter-client==8.6.3
nbclient==0.10.2
nbformat==5.10.4
ipykernel==6.30.0
pyyaml==6.0.2
psutil==7.0.0
numpy==2.2.6
matplotlib==3.10.3
